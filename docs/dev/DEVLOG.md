# DEVLOG — 개발 기록

> 규칙: ① 문서와 충돌 시 코드 우선 → 문서 역반영 항목으로 기록·보고 ② 필수 아님 추가 기능은 기록만 ③ 직접 확인 불가 판정 3회 → 사용자 확인 항목으로 이관.

## 2026-09-10 — 적대 검수 구현 격차 처분: SRS-UI-3 개정(②안)

- **격차**(2026-09-06 적대 검수에서 발견): `types.ts`의 `enterRequested`·`pauseResume`는 sceneState에 핸들러만 있고 발행처 0곳. SRS-UI-3의 "액션 발행" 규범과 어긋남.
- **처분: ②안 — 문서를 코드에 맞춰 개정하고 데드 액션 제거.** ①안(콜백→액션 정합)을 기각한 근거 셋:
  1. **제스처 문맥 충돌(결정적)** — `store.dispatch`는 알림 사이클 재진입 시 액션을 마이크로태스크로 미룬다(`store.ts` `if (this.notifying) enqueue`). 포인터락·`AudioContext.resume()`을 그 경로에 태우면 **SRS-COR-51의 "요청은 반드시 사용자 제스처 핸들러 문맥에서"를 확률적으로 깬다.** 즉 ①안은 규범 하나를 지키려다 더 강한 규범(브라우저 동작에 근거한)을 깨는 거래였다.
  2. **계층 규칙과 충돌** — 입장 시퀀스는 audio(resume) → core(락·전체화면) → DOM(페이드) → scene(teleport) → core(전이)에 걸친다. `core/`는 `scene/`·`audio/`를 import할 수 없으므로(§3), core가 이걸 소유하려면 main의 배선을 core 안에 콜백 주입으로 재구성해야 한다 — 간접층만 한 겹 늘고 동작은 동일.
  3. **실패를 표현할 수 없음** — `dispatch`는 void. "잠금 거부 → 게이트 잔류"(§6.1)와 연타 가드(`enterBusy`)가 갈 곳이 없다. pauseMenu는 `msUntilResumeAllowed()` 조회 콜백이 어차피 필요해 콜백 표면이 사라지지도 않는다.
- **개정 내용(SRS v1.4)**: SRS-UI-3 본문 취소선 + 개정 문구(전달 수단=부트 배선 콜백, **소유권은 불변** — ui는 여전히 브라우저 API 0회 호출). 부수 정정: SRS-COR-50 말미 문장, §4.2 액션 목록 2종 폐기 표기. 신설 요구 ID 없음, 관측 가능한 동작 변화 없음.
- **콜백 예외의 범위를 규범으로 좁힘**: 예외는 ①제스처 태스크를 요구하는 동작 ②쿨다운 잔여시간 같은 조회 — **둘뿐**. 나머지 ui→core 전달은 전부 액션. 이 문장이 없으면 "콜백 허용" 개정이 액션 규율 전체를 무르게 하는 구멍이 된다.
- **재발 방지(신설 TC-UI-07)**: `check-arch`에 **데드 액션 게이트** 추가 — `Action` 유니언 전 멤버 대 `dispatch({type:'…'})` 발행처를 대조해 발행처 0인 멤버에서 빌드 실패. 음성시험 수행(더미 액션 주입 → exit 1, 정상 상태 → OK). 이번 격차는 사람 검수가 3주 뒤에야 잡았지만, 이후로는 커밋 시점에 잡힌다.
- **역정합**: SRS v1.4(UI-3·COR-50·§4.2) · TC_UI_Overlay v1.1(TC-UI-02 재작성·TC-UI-07 신설·다음 번호 8) · CLAUDE.md §2·§5 · core-state 스킬 · `types.ts`·`sceneState.ts`(데드 코드 제거) · gate/pauseMenu/main 주석에 SRS 포인터.
- **부수 발견·수정(도구)**: `generate-report.mjs`가 vitest 진입점을 `<ROOT>/node_modules/vitest/vitest.mjs` 고정 경로로 잡아, **git worktree에서 리포트 게이트가 조용히 실패**하고 있었다(node_modules는 상위 체크아웃에 있음 → "JUnit XML이 생성되지 않았다"). `createRequire` 해석으로 교체 + vitest stderr 전달 추가. 검증 게이트가 환경에 따라 무력화되는 건 게이트가 없는 것보다 나쁘다.
- **남은 것 없음** — 2026-09-06 "처분 결정 필요" 항목은 이로써 종결.

## 2026-08-22 — 개발 착수

### 환경 확인
- three@0.185.1 npm 실존 확인 — SRS 핀과 일치. postprocessing@6.39.4 (peer <0.186.0 충족), detect-gpu@5.0.70.
- git 저장소 초기화 완료. 코드 라이선스: **UNLICENSED(비공개)로 임시 설정** — 공개 라이선스(MIT 등) 결정은 사용자 확인 항목.

### 구현 완료 (2026-08-22)
- 전 모듈 구현: core(store/settings/sceneState/inputSession) · scene(renderer/world/player/screen/quality + 순수 로직 renditionSelect/adaptation) · audio(graph 3버스/ambience 다층 합성 루프/positional HRTF) · ui(gate/settings/pause/credits/unsupported/hud/debugOverlay) · analytics(no-op) · scripts(check-arch/size/licenses, gen-test-media).
- **검증 통과**: tsc strict 0오류 / Vitest 24개 전부 통과(설정 클램프·전이 매트릭스 전수·렌디션 선택·적응 히스테리시스) / check-arch·check-licenses OK / 프로덕션 빌드 **gzip 218.8KB ≤ 300KB(NFR-8)** / 브라우저 실기: 게이트 렌더·inert 패널 격리·gate→corridor→hall→pause→exiting→gate 상태·UI 동기화 E2E 확인·AudioContext running·720p 영상 canplay·절차 셰이더 폴백 및 토스트 동작 확인.

### 사용자 피드백 반영 (2026-08-22 저녁)
- **버그 2건 수정**: ① 오버레이 버튼 전체 클릭 불가(CSS ID 특이도) ② 입장 교착(transitioning 플래그 자기충돌) + 스킵 순간이동 누락 + 입장 시 시선 방향 반전.
- **[제품 결정 변경] 소리는 탭을 볼 때만** — 백그라운드 오디오 지속(구 FR-44/R-7) 폐기, visibilityBus 게인 페이드로 구현. RFP·PRD·SRS·CLAUDE.md 역반영 완료.
- **스크린 비주얼 개선**: 합성 테스트 영상이 사실상 정지 화면이라는 지적 → 눈에 띄게 움직이는 절차 생성 일몰 셰이더(구름 드리프트·바다 스웰·태양 글리터 경로·느린 워밍 브리딩)를 **기본 모드**로 전환. 영상 파이프라인은 `?video`로 테스트 가능 — 실제 영상 소스 채택 시 기본 복귀.
- **공간 웅장함 패스(플레이스홀더 v2)**: 열주 콜로네이드(충돌 포함)·천장 보·복도 리브·입구 포탈·관람 벤치·안개(FogExp2)·빛 속 먼지 입자 500(배경 애니메이션 설정과 연동). 정식 아트는 여전히 M1.

### 사용자 피드백 3차 (복도·시야·소리)
- 소리 과대 → 앰비언스·정위 게인 절반 수준으로 하향.
- **FOV 단위 버그**: 설정 90°를 three.js 수직 fov에 직결 → 어안·상방 왜곡. 가로→세로 변환 도입(90°h ≈ 59°v @16:9).
- 바닥 무인식 → 절차 타일 텍스처(줄눈+톤 변화)·유도선·저각 바닥 조명.
- 복도: 40m→**24m** + 속도 1.2/2.0/3.0(문서 역반영: FR-22, SCN-24), 천장 4.2m(홀 대비로 웅장함 강화), 벽 패널 텍스처·스콘스 조명 12개·중앙 유도선, 안개 0.014→0.008("잘려 보임" 해소), 스크린 하단 2.2→1.2m + 셰이더 수평선 하향(상방 시선감 제거).

### 사용자 결정 v4 — 랩어라운드 월 스크린 (2026-08-22)
- **스크린을 단일 평면 → 입구 제외 홀 벽면 전체(뒷벽 우측→우벽→정면→좌벽→뒷벽 좌측 5세그먼트)로 변경. 직교 벽면 그대로(원통 아님).** 절차 일몰을 월드 좌표 파노라마로 재작성 — 하늘·구름·바다가 벽 모서리를 넘어 연속, 태양에서 멀어질수록 황혼으로 감쇠(좌우 어두움 문제의 근본 해결).
- 영상 모드(?video)는 정면 벽만 재생, 측면은 파노라마 유지 → **실영상 채택 시 파노라마/멀티면 소스 요구가 생김** — 소스 선정 결정(사용자 확인 항목)에 반영 필요. 문서 역반영 후보: PRD FR-30·§6.1(스크린 형태·소스 요구), SRS 스크린 명세.

### 문서 역반영 (코드 우선 판단 — SRS 반영 필요)
1. **SRS-SCN-12**: `SelectiveBloomEffect` 대신 **휘도 임계 BloomEffect**(threshold 0.85 — 발광 스크린만 통과) 채택. 동일 목적을 더 낮은 비용·단순한 구성으로 달성. → SRS에 "또는 휘도 임계 기반 등가 구현 허용" 문구 반영 필요.
2. **SRS-SCN-13**: 스필 라이트 평균색을 GPU 다운샘플+fence 비동기 읽기 대신 **2×2 캔버스 drawImage CPU 샘플(250ms 주기)** 로 구현 — 구현 단순·비용 미미, CORS 오염 감지(ERR-10)도 이 경로에서 겸함. → "등가 CPU 샘플 허용" 문구 반영 필요.
3. **SRS §8.2 renderScale**: 뷰포트 부분 렌더(재할당 금지) 대신 현재 **setSize 재할당 방식** — 변경 빈도가 히스테리시스로 제한돼 실용상 문제 없으나 규범과 다름. M0a에서 스파이크 실측 후 부분 렌더 전환 여부 결정. (규범 유지, 구현 격차로 기록)
4. **detect-gpu 초기 지연 버그 발견·수정**: 네트워크 벤치마크 fetch가 지연/실패하면 렌더러 크기가 0으로 남는 문제 → 생성 즉시 Low 프리셋 적용 + 감지 4초 타임박스. SRS §8.2 "미판정 → Low 시작" 규범을 구현이 강화한 사례.

### 추가 기능 후보 (기록만 — 이번에 안 함)
- 모션 블러 실제 이펙트(토글은 존재, 기본 꺼짐이 규범이라 시각 효과 자체는 미구현 no-op).
- LUT 색보정(High 프리셋 체인의 LUT3D — 아트 패스(M1)에서 LUT 자산과 함께).
- 복도 연출 고도화(현재 스트립 라이트만 — M1 아트).
- Playwright 통합 테스트(§9.2 권장 계층).
- 매니페스트 기반 바이트 가중 진행률(현재 절차 생성 에셋이라 즉시 완료 — 실제 GLB/오디오 도입 시).

## 2026-09-06 — SDLC 방법론 이식 (프로젝트_운영_방법론_이식_가이드라인.md 적용)

- **이식 판정**: 규모 3.7k줄 → 경량판 코어(§E.4) + 스킬층까지 확장(사용자 요청). 기존 `SRS-<모듈>-NN` ID는 append-only 규율로 **유지**(FR 재번호화 기각 — SDD D-09).
- **① 컨텍스트 층**: `CLAUDE.md` 9섹션 전면 개편(주석 트리·Invariants·계약 표면·결정 로그·동기화 규칙 표) + `.claude/skills/` 5종 신설(core-state / scene-video / audio-graph / tc-verification / requirement-docs).
- **② 요구문서 층**: `TC-<모듈>-NN` 가족 신설, `docs/tc/` 6문서(COR·QLT·VID·AUD·SCN·UI — 기준 모델 COR 세트, 총 55 TC 정의). `docs/SDD.md` 신설(설계 결정 기록 D-01~09 — DEVLOG 결정 승격). SRS v1.2 추록(§1.2 시험 ID·§9.5 TC 체계 — 기존 본문 무변경).
- **③ 검증 자동화 층**(pytest→Vitest 이식): 테스트 제목 `[TC-…]` 태깅(24개 전부) + `scripts/generate-report.mjs`(JUnit 집계→TC 문서 대조→`docs/report/ci-report.md`, **orphan=빌드 실패 게이트**, `npm run build`에 편입). 음성 시험으로 게이트 동작 확인(오타 태그 → exit 1).
- **이식 중 잡은 함정 2건**(방법론 §C.7 유형): ① JUnit `classname=`이 `name=` 정규식에 선매칭 → 공백 경계 파싱으로 수정 ② TC 문서의 "다음 번호는 TC-X-NN부터" 문구가 정의로 오집계 → 번호만 표기하는 규칙화(tc-verification 스킬에 기록).
- 검증: vitest 24 passed · orphan 0 · check-arch/licenses OK.

### 적대 검수 (2026-09-06 — 이식 직후 전수 재검)

- **TC 문서 상위 체인 인용 오류 6건 정정**: 초안이 RFP R-번호를 추정 인용(예: 오디오에 R-9 "VR 불필요", 렌디션에 R-8 "링크 입장") → RFP §3 실정의와 전수 대조해 재작성(정위 오디오=R-3, 렌디션=R-19~21 등).
- **[구현 격차 발견 — 문서 역반영 후보]** `types.ts`의 `enterRequested`·`pauseResume` 액션은 sceneState에 핸들러만 있고 **발행처 0곳**. 실제 경로: 입장 = gate `onEnter` 콜백 → main `doEnter`, 일시정지 = Esc → `pointerlockchange` 관측(inputSession). SRS-UI-3의 "enterRequested 액션 발행" 규범과 어긋남 — 브라우저 API 소유권(core 전유)은 준수 중이므로 아키텍처 위반은 아님. ~~**처분 결정 필요**: ① 코드를 규범대로(콜백→액션) 정합 ② SRS-UI-3을 콜백 허용으로 개정 + 데드 액션 제거.~~ **[종결 2026-09-10 — ②안 채택, SRS v1.4·TC-UI-07. 근거는 이 문서 2026-09-10 항목]**
- **리포트 장치 음성시험 추가 수행**: 실패 테스트 → ③섹션 ❌ + 종료코드 전파, 복수 TC-ID 단일 제목 집계, orphan → exit 1 (3건 모두 정상).
- CLAUDE.md §5 표를 실코드 대조로 재작성(발행처 정정), §2 트리에 `data/`·`styles.css` 보강.

## 2026-09-11 (9차) — 소재 사양서(MEDIA-SPEC.md) 분리 + 현장 관찰 추가 반영

- **추가 증언**: 실감1관 스크린은 **천장까지 닿는 면이 아니라 벽면의 '띠'**였고 **입구 쪽까지 이어져** 있었다. v5 설계의 세 선택(띠 0.2~8.2m·둘레 전체 연속·타원)과 독립적으로 일치한다. 조사 L §1.6.1에 대조표로 기록.
- **질문**: "이런 영상에 일반화된 규격과 타입이 있나?"
- **답(두 층위)**: ① **360 촬영 소재**는 표준이 있다 — 에퀴렉트 2:1 + Spherical Video V2(`sv3d`/`st3d`) / OMAF(ISO/IEC 23090-2). ② **띠형 파노라마 스크린의 화면 비율**은 표준이 **없다**(국중박 12:1, 우리 9.68:1 — 건물이 다르므로 있을 수 없다). 이 바닥의 "규격"은 국제 표준이 아니라 **전시장이 제작사에 건네는 사양서**다.
- **산출물**: `docs/prd/research/MEDIA-SPEC.md` — 스톡 구매·전용 제작 양쪽 경로의 요구사항을 한 장으로. 실측 갱신: 둘러싼 각도 **339.1°**(입구 20.9°), 펼친 **77.4m × 8m = 9.68:1**, 수직 화각 **38.3°**, 에퀴렉트 v **0.329~0.542 = 21.3%**, 권장 8K 크롭 **7680×816 = 6.27 Mpix**(4K UHD 8.29보다 가벼움), H.264 불가(4096 상한) → **VP9/AV1 필수**.
- **주의로 남긴 것**: "수평선 띠가 화질의 전부" — 세로 21%만 쓰므로 천정·발밑 화질은 무의미하고, 바닥이 반사하므로 아래쪽 구간에 삼각대·촬영자가 찍힌 소재는 배제.
- 비트레이트·총 용량·성능 여유는 **M0a 실측 대상**으로 남겨 두고 수치를 지어내지 않았다. 코드 변경 없음.

## 2026-09-11 (8차) — 조사 L 정정: 국중박 실감1관은 평면 벽이 아니라 둘러싸는 타원형

- **사용자 지적**: "내가 갔던 국중박에는 타원이었어, 상영이." — **초판이 틀렸다.**
- **무엇이 틀렸나**: 조사 L 초판은 실감1관을 "60m × 5m **평면** 파노라마 벽"으로 적고, 그 위에 "국중박은 둘러싸지 않으므로 에퀴렉트가 불필요하고 §4.7 기하 문제를 겪지 않는다"는 비교표를 세웠다. **박물관 공식 페이지·보도자료·제작사 발표를 전수 재확인한 결과, 어디에도 스크린 형태가 명시돼 있지 않다** — "폭 60m, 높이 5m 파노라마 스크린"이 전부다. 초판은 '파노라마'라는 단어만 보고 평면이라 넘겨짚었다. 출처 없는 추론을 사실처럼 적은 것이 문제의 본질이다.
- **정정 근거**: 공개 자료가 침묵하는 사안에서 **현장 목격이 미확인 추론보다 강하다.** 기하학적으로도 정합적이다 — 60m가 평면이면 전시실이 60m 길이여야 하지만, 둘레 60m 타원이면 약 22m×15m로 일반적인 전시실 규모다.
- **정정이 바꾼 것**: 국중박은 우리와 '다른' 기하가 아니라 **같은** 기하다. 실제 차이는 **콘텐츠 조달 방식**이다 — 국중박은 그 공간 전용으로 주문 제작해 **펼친 벽면 좌표계에 직접 그리고**, 우리는 **구 기준 스톡(에퀴렉트)**을 가져다 얹는다.
- **이 정정이 §4.7에 주는 의미(중요)**: 호 길이 매핑과 방위각 매핑은 **틀리고 맞는 관계가 아니라 두 파이프라인에 각각 대응한다.** 호 길이 = 벽면에 직접 그리는 전용 제작의 좌표계(절차 하늘 SRS-SCN-25가 이것을 쓰는 이유와 동일), 방위각 = 구 기준 촬영물을 얹는 좌표계. 국중박이 왜곡을 겪지 않는 이유는 형태가 달라서가 아니라 **애초에 구 기준 소스를 쓰지 않기 때문**이다. 따라서 **SRS-VID-10(방위각)과 SRS-SCN-25(호 길이)의 공존은 여전히 옳다** — 코드 변경 없음.
- **재발 방지**: 조사 L에 **§1.7 미확인 사항** 절을 신설해 실감1관의 평면 형태·치수·프로젝터 대수·마스터 해상도를 "공개 자료 없음, 추정 금지"로 못박았다.
- 코드 변경 없음. 조사 L §1.5~1.7 신설, 헤더에 정정 2 기록.

## 2026-09-11 (7차) — 사용자 지적으로 발견한 기하 오차: 타원 ≠ 구 (미디어 매핑 방위각 전환)

- **사용자 지적**: "지금 완벽하게 원의 360도도 아니지 않아?" — **정확한 지적이었고 실제 결함이었다.**
- **문제**: 에퀴렉트는 구의 중심에서 본 등각 투영(가로 한 칸 = 경도 한 칸)인데, SRS-VID-10 초판은 파노라마 **호 길이**로 매핑했다(절차 하늘 SRS-SCN-25 규칙을 그대로 가져옴). 타원에서 같은 호 길이는 같은 각도가 아니다 — 실측: 정면·배면 꼭짓점 15.0 m/rad, 측면 11.0 m/rad, 평균 13.08 → **정면 +14.7%(압축) / 측면 −15.9%(확대)**. 절차 무늬는 이 왜곡을 드러내지 않아 v5 내내 발견되지 않았다.
- **수정**: 미디어 가로 매핑을 **중심 기준 방위각**으로 전환. 함정 하나 — 타원에서 **매개변수각 θ ≠ 방위각**(`x=A·cosθ, z=B·sinθ` → 방위 = `atan2(B·sinθ, A·cosθ)`)이라 파노라마 시작 기준도 환산해야 한다. 세로도 공칭 거리 13m 근사를 버리고 **그 점의 실제 중심 거리**(11~15m)로 고도각을 계산하게 바꿨다. `?map=arc`로 구 매핑과 비교 가능. **절차 하늘의 호 길이 규칙은 유지** — 두 매핑 공존은 소재 성격 차이이며 의도된 것.
- **규격 조사(4.8)**: 에퀴렉트 2:1이 사실상 표준이고, Google **Spherical Video V2**(MP4의 `sv3d`/`st3d` 박스), MPEG **OMAF**(ISO/IEC 23090-2)가 정식 규격. VR180·큐브맵/EAC는 부적합. **소재 선정 시 확인할 것은 결국 '가로가 세로의 정확히 2배인가' 하나** — 스테레오(3D) 소재는 두 눈이 겹쳐 담겨 2:1이 아니므로 배제.
- **높이 관련 확인**: 영상에 벽을 맞출 필요 **없다**. 벽 높이가 에퀴렉트에서 **잘라 쓸 구간**을 정할 뿐이다(현재 밴드 8m = 수직 화각 33.1° = 전체 높이의 18.4%).
- 역정합: SRS-VID-10 매핑 규범 개정, 조사 L §4.7·§4.8 신설, CLAUDE.md §7. 테스트 35 passed, orphan 0, gzip 237.3KB.

## 2026-09-11 (6차) — YouTube 360 영상 사용 가능성 조사: 불가 (코드 변경 없음)

- **질문**: YouTube의 360 영상을 가져다 쓸 수 있나.
- **결론: 못 쓴다. 권리 이전에 기술적으로 막혀 있다.** 실행 중인 앱에서 YouTube embed를 붙여 직접 시험 — `contentDocument`는 `null`, 내부 `<video>` 접근은 **SecurityError**, `drawImage(iframe)`은 **TypeError**. 교차 출처 iframe은 CORS로 우회되는 문제가 아니라 문서 자체가 격리되므로, 합법적으로 임베드해도 벽에 텍스처로 올릴 방법이 없다. IFrame Player API는 재생 제어만 주고 픽셀을 주지 않는다.
- **권리 측면**: YouTube 약관은 제3자 다운로드 도구 사용을 금지한다. CC BY 필터가 있지만 그건 창작자의 저작권 허락이고 YouTube와의 접근 약관은 별개다. 둘을 합쳐도 다운로드가 정당화되지 않는다.
- **부수**: YouTube 재인코딩 압축으로 360 4K도 시선 방향 실효 화질이 평면 1080p 수준 — 77m 벽에는 부족.
- **정당한 대안으로 기록**: CC BY로 공개한 360 창작자에게 **원본 마스터를 직접 요청**. 라이선스가 이미 재사용을 허용하고 압축 전 파일이라 화질 문제도 같이 풀린다.
- 조사 L §4.6에 편입. **코드 변경 없음.**

## 2026-09-11 (5차) — 무료 360 "동영상"의 부재 확인 + 정지 파노라마 수면 애니메이션

- **사용자 요구**: "이미지가 아닌 어느정도 움직이기는 해야한다."
- **무료 360 동영상 조사 결과 — 사실상 없다**: Pexels의 `equirectangular`·`360` 검색 결과를 실제로 열어 확인한 바 **대부분이 tiny planet(스테레오그래픽 투영)**이라 에퀴렉트가 아니다. Vecteezy·Videezy는 계정 로그인이 필요해 에이전트가 받을 수 없다(계정 생성은 금지 행위). 진짜 에퀴렉트 360 동영상은 **유료(Getty·iStock·Envato·Adobe)가 현실적인 유일 경로**다. 정지 파노라마는 CC0로 풍부한데(Poly Haven) 동영상만 그렇지 않다.
- **처분 — 정지 파노라마에 실제 움직임 부여**: 에퀴렉트에서 **수평선은 정확히 적도(v=0.5)**라는 성질을 이용해 **그 아래(수면)만** 시간에 따라 변위시킨다. 하늘·지형은 건드리지 않는다 — 움직이는 순간 사진이라는 게 드러나기 때문. `?flow=`로 세기 조절(`?img=` 기본 1).
- **튜닝 실패 1회(기록)**: 초기값이 `sin(u*210)`·진폭 0.0022로 고주파였는데, 파도선이 **지그재그로 깨져 물이 아니라 글리치로 읽혔다**. 둘레에 24·11주기의 넓고 느린 너울 + 진폭 0.0011로 낮춰 해소. 이 상한을 SRS-VID-10에 규칙으로 못박음.
- **노출**: 톤매핑 HDR은 어둡다 — 이 소재는 `?gain=2.2`가 적정.
- **확인 URL**: `?wrap&img=/media/blouberg.jpg&gain=2.2`
- **미결(사용자 판단)**: ① 이 정도 움직임으로 충분한지, 아니면 유료 360 영상을 살지 ② 실사 위 더스트 파티클이 도드라지는 문제(여전히 미조정).

## 2026-09-11 (4차) — 무료 CC0 360 파노라마로 첫 실사 테스트

- **무료 소스 조사 결과**: Pixabay의 `equirectangular` 태그는 **신뢰할 수 없다** — 36건 중 대부분이 드론 촬영·회전 오브젝트이고 진짜 360 파노라마가 거의 없다. 반면 **Poly Haven**(polyhaven.com)은 **CC0**(상업 이용 가능, 출처 표기 불필요 — 공식 라이선스 페이지 확인)로 sunrise-sunset 태그 HDRI 55건 이상, 최대 16K~24K 에퀴렉트를 제공한다.
- **선정(테스트용)**: `blouberg_sunrise_2` (Blouberg Sunrise 2 — 케이프타운 해변·바다·테이블마운틴, 저자 Greg Zaal). 톤매핑 JPG 3.1MB, **8192×4096 = 정확히 2:1**(파일 헤더로 확인). `public/media/blouberg.jpg`(gitignore 대상).
- **정지 파노라마 경로 추가**: 영상 이전에 소재를 보는 최소 비용 경로로 `?img=`를 SRS-VID-10에 편입. 디코드 루프도 루프 이음매도 없다.
- **실기 확인**: 벽 전체에 실사 파노라마가 붙고 **수평선이 눈높이**, **바닥 반사가 벽과 일치**. 해변에 서 있는 것처럼 읽힌다.
- **발견 — 노출**: 톤매핑된 HDR 마스터는 원본 그대로면 절차 하늘보다 **현저히 어둡다**(셰이더 노출 1.15는 절차 기준으로 튜닝된 값). 소스마다 다르므로 `?gain=` 트림을 신설했다. 이 소재는 **gain≈2.2**에서 적정.
- **미결(사용자 판단)**: ① 실사 위에서는 더스트 파티클이 도드라져 거슬린다 — 절차 하늘 기준으로 튜닝된 값이라 미디어 모드에서 낮추는 것이 맞아 보이나 연출 취향 문제 ② 정지 이미지라 벽에 **움직임이 전혀 없다** — 창밖 풍경처럼 읽을지, 영상이 필요한지 판단 필요 ③ 실제 채택 시 `credits.json` 대장 등재(CC0라 법적 의무는 없으나 프로젝트 규약상 출처 관리 대상).

## 2026-09-11 (3차) — 사용자 방향 확정: 짧은 몰입 + 360 랩어라운드 미디어(SRS-VID-10) 구현

- **사용자 방향**: "10분 이상의 영상을 틀어주지는 않을 것 같다. 잠깐 몰입감 있게 실제 밖을 힐링되도록 보는 정도." → 조사 L의 **B1**(실사 360 클립 1개를 벽 전체에 루프).
- **확인된 정합**: 이 방향은 **PRD FR-37(상영관 도달 후 2분 내 감상 사이클 1회 완결 — "짧게 머물러도 완결감", MR-12·R-7)과 정확히 일치**한다. 10분 순환(FR-39)은 v5 방향 전환 때 나중에 얹힌 요구이며, 몇 분 체류 관람자는 그 여정의 일부만 본다.
- **구현 — SRS-VID-10 신설**: `src/scene/panoMedia.ts`. 쿼리 `?wrap`으로 소스를 에퀴렉트로 해석해 벽 전체에 매핑. 가로 = `fract(호 길이/타원 둘레)`(실제 호 길이 — 장축 늘어짐 없음, 입구 틈은 해당 구간 이미지를 가릴 뿐), 세로 = 눈높이 고도각 `v = 0.5 − atan((y−eyeY)/d)/π`(d=13m 공칭) — **수평선이 관람자 눈높이에 놓인다**. 미디어 유니폼은 하늘 유니폼과 같은 **공유 IUniform 객체**라 벽·바닥·천장이 한 집합을 읽는다(SRS-SCN-26 불일치 금지). 루프는 기존 A/B 스왑을 재사용하되 **반사는 교차 구간에 슬롯 A만** 샘플링한다 — 러프니스 3탭이 6회 텍스처 읽기로 불어나는 것을 막는 의도적 허용 오차(어둡고 흐린 2차 이미지라 0.25s 지연은 보이지 않는다). 미디어 진입·이탈은 0.9s 페이드(하드 컷 금지, SRS-SCN-23).
- **실기 확인**: 수평선·방위 눈금이 든 1024×512 테스트 에퀴렉트(GIF)로 검증 — 수평선이 **눈높이에서 타원 둘레를 따라 끊김 없이 이어지고**, 정면·배면 모두 **바닥 반사가 벽과 일치**했다. 텍스처 업로드 9회/초로 GIF 10fps와 일치(게이팅 정상). 번들 gzip 236.0KB/300KB, 테스트 35 passed, orphan 0.
- **GIF 부적합의 추가 실증**: 2048×1024·4초 테스트 GIF가 **52MB**가 되어 dev 서버가 500으로 거절했다(1024×512·3초도 5.4MB). 조사 L §3.4의 결론(상영 포맷 후보 아님)이 실물로 확인됨.
- **미결 — 사용자 결정 필요**: 실사 루프가 **배포 기본값**이 되면 PRD **FR-39(MUST, 10분 순환)**의 지위를 정리해야 한다. 현재는 아무것도 삭제하지 않았다 — 절차 하늘 순환이 여전히 기본값이자 미디어 실패 시 폴백이고, `?wrap`은 그 위에 얹히는 모드다. FR-39를 내리려면 PRD 개정(append-only: 취소선 + 대체 포인터)과 SRS-VID-8·TC-SCN-12~14 처분이 함께 간다.
- **다음**: 실제 360 일몰 클립 선정(라이선스 확인 필요 — 사용자 결정 항목 3번과 연결). 선정 후 밴드 사전 크롭(7680×816, VP9/AV1) 인코딩 + M0a에서 디코드·반사 비용 실측.

## 2026-09-11 (2차) — 홀 입장 순간이동 제거 + 조사 L(초광폭 미디어 포맷)

- **입장 순간 "끊기는 느낌" 원인 확정**: `TRIGGER_hallEntry`(z ≤ −11.5)가 `BOUNDS_viewing`(z ≤ −12)보다 **0.5m 앞에서** 끝나 있었다. 그래서 도보로 도달하면 전이 시점의 위치가 항상 `BOUNDS_viewing` 밖 → "홀 밖에 있다"는 안전망이 발동해 `SPAWN_hall`(z=−15.5)로 재배치 → **매번 4.05m 순간 전진**. 브라우저에서 전이 전후 z를 직접 찍어 4.05m를 실측했다.
- **처분**: ① 트리거를 `BOUNDS_viewing` 내부로 이동(z ∈ [−14.5, −12.3]) ② 재배치를 **게이트 직행(`from === 'gate'`)과 범위 이탈에만** 한정. 재확인 결과 z=−11.5 복도 유지, z=−12.4 전이·위치 보존(순간이동 0). SRS-SCN-24에 위치 규칙으로 못박음.
- **조사 L 수행** — 사용자 질문 "국중박은 애초에 영상을 길게 만든 건가?". 답: **그렇다.** 실감1관은 60m×5m(12:1) 공간에 맞춘 전용 마스터를 제작하며, 〈강산무진도〉는 원작 두루마리 자체가 8m56cm×44.1cm(≈19:1)다. 우리 기하(전면 리본 24m×8m=3:1/111.6°, 전체 벽 77.4m×8m=9.67:1/348°)로 환산해 16:9 소재의 크롭률(41%/82%)과 필요 해상도(전면 4K=34px/deg, 전체 벽 8K=22px/deg)를 계산했고, 브라우저 디코더 가로 상한(H.264 4096 / VP9·AV1 8192)이 초광폭 단일 파일을 막는다는 점을 확인했다. GIF는 팔레트 256색·프레임 간 압축 없음(테스트 GIF 960×540·4초에 8.9MB)으로 **상영 포맷 후보에서 제외, 소재 확인용으로 한정**.
- **조사 L 초판 권고 철회 (같은 날, 사용자 반론 "스톡이 풍부하면 왜 A가 권고냐")**: A안 근거 3개 중 2개가 부당했다 — "관람자는 전면을 본다"는 **360° 랩어라운드라는 v5 전제와 정면 모순**(v4 직교 5세그먼트를 기각한 바로 그 이유)이고, "코드 변경 0"은 제품 논거가 아닌 구현 편의다. 게다가 초판은 A안의 실사/절차 **경계 이음매** 결함을 적어 놓고도 A를 권고했다.
- **재계산이 뒤집은 것**: B안의 비용으로 잡았던 "8K 디코드"는 에퀴렉트를 통째로 재생한다는 가정이었다. 밴드가 덮는 수직 화각이 38.3°(세로의 21%)뿐이므로 그 띠만 사전 크롭하면 **7680×816 = 6.27 Mpix — 4K UHD(8.29 Mpix)보다 가볍다.** 화질 99 px/m 유지, 대역폭 낭비 0. 단 가로 7680은 H.264 상한(4096) 초과라 VP9/AV1 필수.
- **진짜 쟁점으로 좁혀짐**: 스톡 360 일몰은 많지만 **표준 클립 길이가 10~30초의 한 순간**이라 10분 여정(FR-39/SRS-VID-8)을 살 수 없다. 그래서 B는 B1(한 순간 고정·FR-39 폐기)/B2(위상별 클립 교차 페이드 — 기존 A/B 스왑 재사용)/B3(자체 제작)으로 갈린다.
- **대기 중 사용자 결정**: **10분 여정을 지킬 것인가**가 갈림길. 지키고 미디어 예산 없음 → A안(+경계 이음매 보정 선결), 지키고 예산 있음 → B2/B3, 여정을 접음 → B1(PRD FR-39 개정 사안). B 계열이면 `aArc` 에퀴렉트 매핑 + 반사 정합(조사 L §4.4)이 따라온다.

## 2026-09-11 — 사용자 시연 피드백 5건 반영 (음량·동선·문턱·반사·화면 높이) + GIF 상영 모드

사용자가 브라우저에서 직접 관람한 뒤 보고한 항목을 원인까지 확인하고 처분했다.

1. **음량 1/10** — `graph.ts`에 `OUTPUT_TRIM`(=0.1)을 신설해 `userVolumeBus`에 곱했다. 레이어 게인표(DAY/NIGHT/CEILING)는 손대지 않았다 — 상대 믹스가 TC-AUD-09~10으로 고정돼 있고, 보정을 표에 흩뿌리면 다음 조정 때 어느 값이 연출이고 어느 값이 보정인지 구분할 수 없게 된다. 이미 저장된 `masterVolume`이 있어도 즉시 적용된다(슬라이더 0~1 전 구간 유지).
2. **홀까지 너무 멂** — `CORRIDOR_LENGTH` 24m → 12m. 2.0 m/s 기준 약 12초 → 5초.
3. **문턱에서 검은 줄무늬** (원인 확정) — 복도 슬래브(top y=0, z∈[−24,0])·문턱 브릿지(top y=0, z∈[−25,−22.6])·홀 원반(y=0)이 **셋 다 y=0 평면에 겹쳐** 깊이 다툼을 일으켰다. 브릿지를 문 선에서 시작하도록 줄이고(z∈[−13.6,−12]) 1cm 낮췄다. 브라우저에서 세 면의 top y/z 범위를 직접 질의해 겹침 해소를 확인.
4. **발밑의 큰 반원 암부** (원인 확정) — 해석적 반사에서 광선이 밴드 위로 벗어나면 `vec3(0.0)`을 반환하던 코드. 관람자 근처일수록 반사각이 가팔라 밴드를 넘기므로 발밑에 큰 원호형 검정이 생겼다. 세로 좌표를 밴드 끝으로 **클램프 + 거리 감쇠**로 바꿔, 근거리에서는 천정 쪽 하늘이 이어지는 잔잔한 수면으로 읽힌다. SRS-SCN-26에 규칙으로 못박음.
5. **화면 높이 상향** — 밴드 0.25~6.25m(6m) → 0.2~8.2m(8m), 천장 8.0 → 9.4m. 부수 효과로 4번의 밴드 이탈 영역도 줄어든다.

**추가 — GIF 상영 모드(SRS-VID-9)**: "지금 보이는 게 이미지냐"는 질문의 답은 **둘 다 아니다 — 매 프레임 GLSL로 그리는 절차 하늘**이다. 실영상 도입 전 소재를 눈으로 확인하려는 요구에 맞춰 `?gif`(기본 `/media/sunset.gif`, `?gif=<url>`로 지정 가능) 경로를 신설했다. GIF는 `<video>`에 실을 수 없어 WebCodecs `ImageDecoder`로 **프레임을 1장씩 지연 디코드**해 캔버스→`CanvasTexture`로 올린다(고화질 GIF 전 프레임 상주는 메모리 예산을 넘긴다). 루프 소유·업로드 게이팅은 SRS-VID-2·3 원칙 그대로. 영상·GIF 공통으로 **커버 맞춤**(`uUvScale`/`uUvOffset`)을 넣어 16:9 소재가 24m×8m 리본에 늘어붙지 않게 했다.

**실기 확인**(Chrome, 브라우저 패널): 문턱 줄무늬·발밑 반원 소멸, 화면 높이 상향, GIF 960×540/48프레임 상영 확인 — 텍스처 업로드 11회/초로 GIF 12fps와 일치(rAF 60회 아님 → 게이팅 정상). 콘솔 오류 0.

**역정합**: SRS v1.4(VID-9 신설, SCN-25·26·AUD-1 개정) · CLAUDE.md §2 트리·§7 정책 3항 · 본 문서. 시험: 35 passed, orphan 0 (순수 함수 계약 무변경 — 이번 변경은 셰이더·지오메트리·버스 상수라 신규 TC 없음).

## 2026-09-06 — 사용자 결정 v5: 타원 몰입 홀 전면 개편

- **결정**: "국중박 디지털 실감영상관처럼 입구 제외 360° 벽 전체가 스크린인 공간" — v4(직교 5세그먼트)로는 부족하다는 사용자 판정. 조사 K(실감1관 60m×5m 파노라마·2022 바닥 투사 확대·아르떼뮤지엄·회복 환경 연구·박명 단계) 수행 후 방법론 역반영 6단계로 진행.
- **구현**: ① 타원 홀 15×11m(둘레 ≈82m) + 입구 제외 연속 리본 스크린(밴드 0.25~6.25m, 파노라마 좌표=실제 호 길이) ② 해석적 반사 바닥·천장(추가 렌더 패스 0 — 뷰 광선 대칭→타원 실린더 교차→동일 panorama() 평가, High 3탭/Low 1탭·천장 off) ③ 10분 하늘 순환(골든→일몰→블루→별밤→새벽, 채널 ≤5%/s·고도 ≤0.35°/s, 무이음 루프) + 2분 마이크로 사이클 유지 ④ 앰비언스 5레이어 위상 연동(gainsForElevation — 밤 총합 ≤ 낮) + 벽면 정위 앵커 5소스 ⑤ 선택형 호흡 리듬 빛(breathGuide, 기본 off, 분당 6회) ⑥ 기둥·보 제거(시선 차폐 0), 소프트 스프라이트 더스트.
- **역정합**: RFP v1.3(R-26~28)·PRD v1.2(FR-38·39·54)·SRS v1.3(SCN-25·26, VID-8, AUD-9, COR-25)·SDD D-10~14(D-02 Superseded)·TC(SCN-08~14, AUD-09~10, COR-16)·조사 K 신설·CLAUDE.md·스킬 3종.
- **시험 규범 충돌 1건**: 새벽 키프레임(570→600s)의 고도 기울기 0.4°/s가 SRS-VID-8 상한(0.35) 초과 — 시험을 약화하지 않고 키프레임을 560s로 수정해 해소(TC-SCN-13).
- **실기 확인**(Chrome, forceHall 훅): 골든아워·측면 연속·블루아워·별밤 4장면 + Low/High 프리셋 전환 렌더 정상, 콘솔 오류 0. fps 정량은 M0a(개발 패널 rAF 스로틀로 본 세션 측정 불가). 노출 1.6→1.15 하향(반사가 광량을 더함), 태양 2로브 글로우·소프트 더스트 보정.
- **후속(사용자 확인 항목 추가)**: 실영상 채택 시 정면 24m 리본만 영상 — 파노라마 소스 요구는 SRS §11-10.

### 사용자 확인 필요 항목
1. **실제 관람 검증 (환경 제약 3회로 이관)**: 이 세션의 브라우저 패널이 화면에 표시되지 않아 rAF(렌더 루프)·Pointer Lock·스크린샷 검증이 불가했음(비합성 탭에서는 브라우저가 이들을 정지시킴). 상태 머신·UI 동기화·오디오·영상 로드는 콘솔로 검증 완료. **아래 체크리스트로 직접 확인 필요**:
   ```bash
   npm run dev
   ```
   - [ ] http://localhost:5173 접속 → 게이트 표시, "입장하기" 클릭 → 마우스 잠금 + 어두운 복도에서 시작, 소리(파도/바람 합성음)가 앞쪽에서 들림
   - [ ] WASD/방향키 이동, 마우스·Q/E/R/F 시점, 복도 끝 도달 시 상영관 자동 진입
   - [ ] 상영관: 대형 스크린에 일몰(테스트 영상 또는 그라데이션) 상영, 다가가면 소리 커짐
   - [ ] Esc → 일시정지 메뉴(소리 볼륨 낮아짐), 1.5초 후 "계속" 활성 → 복귀
   - [ ] 탭 전환 → 소리 유지, 복귀 시 "클릭하여 시점 복귀" 프롬프트
   - [ ] M 음소거, 설정 변경 즉시 반영·새로고침 후 유지, ?debug 오버레이 fps 표시
2. ~~**코드 라이선스 결정** (현재 UNLICENSED/private).~~ **[종결 2026-09-10 — MIT 채택.** 원격 저장소(github.com/rehv-k/immersive-healing) 생성 시 사용자가 MIT를 선택했고, 그 `LICENSE`(Copyright 2026 rehv)를 저장소 대장으로 삼아 `package.json`의 `license`를 MIT로 정합했다. `private: true`는 **유지** — 이건 npm 레지스트리 실수 배포 방지 플래그이며 소스 공개 여부와 무관하다(이 프로젝트는 npm 패키지로 배포하지 않는다).**]**
3. **실제 일몰 영상·사운드 소스 채택**: 현재 합성 테스트 미디어(ffmpeg 절차 생성 + 런타임 합성 앰비언스). Pexels/Pixabay·Freesound 소스 선정은 라이선스 확인이 필요한 사용자 결정 (PRD §6.1 기준 ⑥ 태양 이동 포함).
